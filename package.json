{
  "name": "loan-tracker",
  "version": "1.1.0",
  "private": true,
  "packageManager": "pnpm@11.3.0",
  "scripts": {
    "dev": "turbo run dev",
    "dev:web": "pnpm --filter @loan/web dev",
    "dev:mobile": "pnpm --filter @loan/mobile start",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
